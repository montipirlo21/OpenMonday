query GetBoardsStructureById($ids: [ID!]!) {
  boards(ids: $ids, state: all) {
    id
    name
    updated_at
    items_count
    columns {
      id
      title
      type
      settings
    }
    groups {
      title
      id
    }
  }
}
